# Top-level project fields.
name = "voice-bot"                                      # required: the workspace label
group = "ICICI"
description = "dev project to transcribe and extract meaningful insights from currency exchange call recordings"             # shown in the browser
working_dir = "${HOME}/Documents/voice-bot/"    # ~ and $VARS expand

[[tabs]]
name = "nvim"                                            # required: the tab label
command = "source .venv/bin/activate && nvim" # runs on startup

[[tabs]]
name = "pi"

[[tabs.panes]]
command = "pi"

[[tabs.panes]]
command = "ll"
split = "right"

[[tabs]]
name = "docker-comp"
command = "ll"

[[tabs]]
name = "terminal"                                          # no command — just an empty shell
command = "ll"

