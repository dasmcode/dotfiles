# Top-level project fields.
name = "Documents 󱔗 "                                      # required: the workspace label
group = "personal"
working_dir = "${HOME}/Documents/"    # ~ and $VARS expand

[[tabs]]
name = "terminal"                                          # no command — just an empty shell
command = "ll"
