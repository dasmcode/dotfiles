# Top-level project fields.
name = "nvim config  "                                      # required: the workspace label
group = "personal"
working_dir = "${HOME}/.config/nvim/"    # ~ and $VARS expand

[[tabs]]
name = "nvim"                                            # required: the tab label
command = "nvim init.lua" # runs on startup

[[tabs]]
name = "terminal"                                          # no command — just an empty shell
command = "ll"

