# Top-level project fields.
name = "herdr-plus 󰳆 "                                      # required: the workspace label
group = "personal"
working_dir = "${HOME}/.config/herdr/plugins/config/cloudmanic.herdr-plus/"    # ~ and $VARS expand

[[tabs]]
name = "nvim"                                          # no command — just an empty shell
command = "nvim"

[[tabs]]
name = "terminal"                                          # no command — just an empty shell
command = "ll"
